<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pctol.server.mapper.TopicExcelMapper">
    <insert parameterType="com.example.pctol.pojo.entity.TopicExcel" id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
       insert into topic_excel(name,url,launcher,create_time,error_msg)
       values (#{name},#{url},#{launcher},#{create_time},#{errorMsg})
    </insert>
</mapper>